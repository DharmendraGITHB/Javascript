// Find the longest word in a string

function findLongestWordLength(str) {
  return str.length;
}

let long = findLongestWordLength("The quick brown fox jumped over the lazy dog");

console.log(long)