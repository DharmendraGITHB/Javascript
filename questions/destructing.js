// swapping, 
let a = 20;   //using semicolon is good practice
let b = 23;   // automatically assigned semicolon throw an error in some cases

// destructuring assignment
[a,b] = [b,a];

console.log(` After swapping the value of a : ${a}`)
console.log(` After swapping the value of b : ${b}`)

